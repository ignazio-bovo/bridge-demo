manifestVersion: subsquid.io/v0.1
name: datura-bridge-backend
version: 1
description: Small indexing backend for the datura bridge frontend
build: 
deploy:
  addons:
    postgres: 
    # rpc:
    #   - eth.http
    #   - bittensor.http
  processor:
    - name: eth-processor
      cmd: [ "sqd", "process:eth" ]
    - name: subtensor-processor
      cmd: [ "sqd", "process:subtensor" ]
  api:
    cmd:
      - sqd
      - serve:prod
